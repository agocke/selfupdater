<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <!-- Designed to be consumed by single-file / Native AOT apps. -->
    <IsAotCompatible>true</IsAotCompatible>
    <!-- Required by [LibraryImport]'s source-generated marshalling (the rename(2) call
         that flips the POSIX version pointer atomically). -->
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <!-- Doc comments are provided for the API surface that matters; don't fail on
         every member (records, enum cases, ctors) lacking one. -->
    <NoWarn>$(NoWarn);CS1591</NoWarn>

    <!-- Package metadata -->
    <PackageId>SelfUpdater</PackageId>
    <Version>0.5.0</Version>
    <Authors>Andy Gocke</Authors>
    <Description>A small, pluggable self-update engine for single-file .NET apps. Checks a version, downloads the matching build, verifies and validates it, then replaces the running executable in place. Where updates come from is pluggable (GitHub releases, a directory/share, or your own source).</Description>
    <PackageTags>self-update;updater;single-file;aot;auto-update</PackageTags>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <PackageProjectUrl>https://github.com/agocke/selfupdater</PackageProjectUrl>
    <RepositoryUrl>https://github.com/agocke/selfupdater</RepositoryUrl>
    <RepositoryType>git</RepositoryType>

    <!-- Build provenance + debugging: SourceLink, deterministic, embedded untracked
         sources, and a snupkg symbol package. ContinuousIntegrationBuild is set by CI. -->
    <PublishRepositoryUrl>true</PublishRepositoryUrl>
    <EmbedUntrackedSources>true</EmbedUntrackedSources>
    <IncludeSymbols>true</IncludeSymbols>
    <SymbolPackageFormat>snupkg</SymbolPackageFormat>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="SelfUpdater.Tests" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Semver" Version="3.0.0" />
    <PackageReference Include="serde" Version="0.13.0" />
    <PackageReference Include="Microsoft.SourceLink.GitHub" Version="8.0.0" PrivateAssets="all" />
  </ItemGroup>

  <ItemGroup>
    <None Include="..\..\README.md" Pack="true" PackagePath="\" />
  </ItemGroup>
</Project>
